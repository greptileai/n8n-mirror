allOf:
  - $ref: './create-credential-response.yml'
  - type: object
    required:
      - projects
    properties:
      projects:
        type: array
        description: Projects this credential is shared with (id, name, role, createdAt, updatedAt)
        items:
          $ref: './credentialListProject.yml'
